# ⚡ البدء السريع - نظام التحديث

## 🎯 للمطور (خطوة واحدة فقط!)

### 1. بناء updater.exe (مرة واحدة فقط):
```bash
# شغل الملف ده وخلاص
build_updater.bat
```

### 2. إعداد GitHub (مرة واحدة فقط):

**أ. أنشئ ملف `version.json` في الـ repo:**
```json
{
  "version": "1.0.0",
  "url": "https://github.com/imhzm/SkyWaveERP/releases/download/v1.0.0/update.zip"
}
```

**ب. عدل `version.py` وحط الرابط الصح:**
```python
UPDATE_CHECK_URL = "https://raw.githubusercontent.com/imhzm/SkyWaveERP/main/version.json"
```

---

## 🚀 إصدار تحديث جديد (كل مرة)

### الخطوات:

1. **عدل رقم الإصدار:**
   ```python
   # في version.py
   CURRENT_VERSION = "1.0.1"  # غير الرقم
   ```

2. **اضغط الملفات:**
   - اضغط كل ملفات المشروع في `update.zip`
   - متضمنش قاعدة البيانات أو __pycache__

3. **ارفع على GitHub:**
   - روح Releases → Create new release
   - حط Tag: `v1.0.1`
   - ارفع `update.zip`
   - انسخ رابط التنزيل

4. **حدث version.json:**
   ```json
   {
     "version": "1.0.1",
     "url": "الرابط اللي نسخته"
   }
   ```

5. **خلاص!** 🎉

---

## 👤 للمستخدم (سهل جداً!)

1. افتح البرنامج
2. روح **الإعدادات**
3. اضغط تاب **"التحديثات"** 🔄
4. اضغط **"التحقق من التحديثات"** 🔍
5. لو لقى تحديث:
   - اضغط **"تنزيل التحديث"** ⬇️
   - استنى شوية
   - اضغط **"تثبيت التحديث"** 🚀
6. البرنامج هيقفل ويفتح تاني لوحده ✨

---

## 🧪 اختبار سريع

```bash
python test_update_system.py
```

لازم كل الاختبارات تنجح (6/6) ✅

---

## ❓ مشاكل شائعة

### "updater.exe مش موجود"
```bash
build_updater.bat
```

### "مش عارف يتصل بالخادم"
- تأكد من رابط UPDATE_CHECK_URL
- تأكد من وجود version.json على GitHub
- شوف النت شغال ولا لأ

### "التنزيل فشل"
- تأكد من رابط التنزيل في version.json
- تأكد من وجود update.zip في Releases
- شوف في مساحة على الهارد

---

## 📞 محتاج مساعدة؟

- 📧 skywaveads@gmail.com
- 📱 +201067894321
- 📱 +201021965200

---

**Sky Wave ERP** 🚀
