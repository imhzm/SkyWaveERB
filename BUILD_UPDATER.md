# 🔧 دليل بناء نظام التحديث التلقائي

## 📋 الخطوات المطلوبة لتفعيل نظام التحديث

### 1️⃣ تحويل updater.py إلى ملف تنفيذي (مرة واحدة فقط)

يجب تحويل `updater.py` إلى ملف `updater.exe` ووضعه في نفس مجلد البرنامج الأساسي.

```bash
# تثبيت PyInstaller إذا لم يكن مثبتاً
pip install pyinstaller

# تحويل updater.py إلى EXE
pyinstaller --onefile --noconsole --name updater updater.py

# نسخ updater.exe من مجلد dist إلى المجلد الرئيسي
copy dist\updater.exe .
```

**ملاحظة:** يمكنك أيضاً استخدام `--console` بدلاً من `--noconsole` إذا أردت رؤية رسائل التحديث.

---

### 2️⃣ إعداد ملف version.json على GitHub

1. **إنشاء ملف version.json:**
   - أنشئ ملف `version.json` في الـ repository الخاص بك
   - استخدم المحتوى التالي كنموذج:

```json
{
  "version": "1.0.1",
  "url": "https://github.com/imhzm/SkyWaveERP/releases/download/v1.0.1/update.zip"
}
```

2. **رفع الملف على GitHub:**
   - ارفع `version.json` في المجلد الرئيسي للـ repo
   - احصل على رابط الـ Raw للملف (مثال):
     ```
     https://raw.githubusercontent.com/imhzm/SkyWaveERP/main/version.json
     ```

3. **تحديث رابط الفحص في البرنامج:**
   - افتح ملف `version.py`
   - عدل السطر:
     ```python
     UPDATE_CHECK_URL = "https://raw.githubusercontent.com/imhzm/SkyWaveERP/main/version.json"
     ```

---

### 3️⃣ إصدار تحديث جديد

عند كل تحديث جديد، اتبع الخطوات التالية:

#### أ. تحديث رقم الإصدار في الكود
```python
# في ملف version.py
CURRENT_VERSION = "1.0.1"  # غير الرقم للإصدار الجديد
```

#### ب. تجهيز ملفات التحديث

**الطريقة 1: تحديث الكود فقط (للمستخدمين الذين لديهم Python)**
```bash
# اضغط جميع ملفات المشروع في ملف ZIP
# تأكد من تضمين:
# - جميع ملفات .py
# - مجلد services/
# - مجلد ui/
# - مجلد core/
# - مجلد assets/
# - requirements.txt
# - version.py (بالرقم الجديد)

# لا تضمن:
# - مجلدات __pycache__
# - ملفات .pyc
# - مجلد .git
# - قاعدة البيانات (skywave_local.db)
```

**الطريقة 2: تحديث EXE كامل (للمستخدمين بدون Python)**
```bash
# 1. بناء البرنامج كـ EXE
pyinstaller --onefile --windowed --icon=icon.ico --name SkyWaveERP main.py

# 2. اضغط الملف التنفيذي والملفات المطلوبة
# تأكد من تضمين:
# - SkyWaveERP.exe
# - مجلد assets/
# - skywave_settings.json
# - أي ملفات DLL مطلوبة
```

#### ج. رفع التحديث على GitHub Releases

1. **إنشاء Release جديد:**
   - اذهب إلى صفحة Releases في الـ repo
   - اضغط "Create a new release"
   - أدخل Tag version (مثل: `v1.0.1`)
   - أدخل عنوان الإصدار (مثل: "الإصدار 1.0.1")

2. **رفع ملف ZIP:**
   - ارفع ملف `update.zip` في قسم Assets
   - انسخ رابط التنزيل المباشر للملف

3. **تحديث version.json:**
   - عدل ملف `version.json` في الـ repo:
   ```json
   {
     "version": "1.0.1",
     "url": "https://github.com/imhzm/SkyWaveERP/releases/download/v1.0.1/update.zip"
   }
   ```
   - احفظ التغييرات (Commit)

---

### 4️⃣ اختبار نظام التحديث

1. **افتح البرنامج**
2. **اذهب إلى الإعدادات → تاب التحديثات**
3. **اضغط "التحقق من التحديثات"**
4. **إذا وجد تحديث:**
   - اضغط "تنزيل التحديث"
   - انتظر حتى يكتمل التنزيل
   - اضغط "تثبيت التحديث"
   - سيتم إغلاق البرنامج وإعادة تشغيله تلقائياً

---

## 🔍 استكشاف الأخطاء

### المشكلة: "لم يتم العثور على updater.exe"
**الحل:**
- تأكد من وجود `updater.exe` في نفس مجلد البرنامج الأساسي
- أعد بناء `updater.exe` باستخدام PyInstaller

### المشكلة: "فشل الاتصال بالخادم"
**الحل:**
- تأكد من صحة رابط `UPDATE_CHECK_URL` في `version.py`
- تأكد من أن ملف `version.json` موجود على GitHub
- تحقق من اتصال الإنترنت

### المشكلة: "فشل التنزيل"
**الحل:**
- تأكد من صحة رابط التنزيل في `version.json`
- تأكد من أن ملف `update.zip` موجود في GitHub Releases
- تحقق من مساحة القرص المتاحة

### المشكلة: "فشل فك الضغط"
**الحل:**
- تأكد من أن ملف ZIP غير تالف
- تأكد من صلاحيات الكتابة في مجلد البرنامج
- أغلق أي برامج قد تستخدم ملفات البرنامج

---

## 📝 ملاحظات مهمة

1. **النسخ الاحتياطي:**
   - دائماً أنشئ نسخة احتياطية قبل التحديث
   - البيانات في قاعدة البيانات لن تتأثر

2. **الصلاحيات:**
   - قد تحتاج صلاحيات المسؤول (Administrator) لتحديث الملفات
   - تأكد من إغلاق جميع نوافذ البرنامج قبل التحديث

3. **التوافق:**
   - تأكد من توافق الإصدار الجديد مع قاعدة البيانات الحالية
   - اختبر التحديث على بيئة تجريبية أولاً

4. **الأمان:**
   - استخدم HTTPS في روابط التحديث
   - تحقق من صحة الملفات المحملة

---

## 🎯 نصائح للإصدارات المستقبلية

1. **إضافة ملاحظات الإصدار:**
   - أضف حقل `release_notes` في `version.json`
   - اعرض الملاحظات للمستخدم قبل التحديث

2. **التحقق من التوافق:**
   - أضف حقل `min_version` لتحديد الحد الأدنى للإصدار المطلوب
   - امنع التحديث إذا كان الإصدار الحالي قديماً جداً

3. **التحديثات التلقائية:**
   - أضف خيار "التحقق التلقائي عند بدء التشغيل"
   - أرسل إشعارات عند توفر تحديثات جديدة

4. **التوقيع الرقمي:**
   - وقع ملفات التحديث رقمياً للأمان
   - تحقق من التوقيع قبل التثبيت

---

## ✅ قائمة التحقق السريعة

قبل إصدار تحديث جديد، تأكد من:

- [ ] تحديث `CURRENT_VERSION` في `version.py`
- [ ] بناء ملف `update.zip` بجميع الملفات المطلوبة
- [ ] رفع `update.zip` على GitHub Releases
- [ ] تحديث `version.json` برقم الإصدار والرابط الجديد
- [ ] اختبار التحديث على جهاز تجريبي
- [ ] التأكد من وجود `updater.exe` في مجلد البرنامج

---

**تم إنشاء هذا الدليل بواسطة Kiro AI** 🤖
