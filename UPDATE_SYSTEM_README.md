# 🔄 نظام التحديث التلقائي - Sky Wave ERP

## 📋 نظرة عامة

تم تطبيق نظام تحديث تلقائي متكامل يسمح بتحديث البرنامج بضغطة زر واحدة، دون الحاجة لتنزيل ملفات يدوياً أو إعادة التثبيت.

---

## 🎯 المميزات

✅ **تحديث تلقائي بالكامل** - فحص وتنزيل وتثبيت من داخل البرنامج  
✅ **تحديث في نفس المكان** - لا حاجة لإعادة التثبيت  
✅ **آمن 100%** - جميع البيانات محفوظة  
✅ **واجهة سهلة** - تاب مخصص في الإعدادات  
✅ **شريط تقدم** - متابعة عملية التنزيل  
✅ **إعادة تشغيل تلقائية** - البرنامج يفتح تلقائياً بعد التحديث

---

## 📁 الملفات المضافة

### 1. الملفات الأساسية
- **`version.py`** - رقم الإصدار الحالي ورابط الفحص
- **`updater.py`** - البرنامج المستقل لتطبيق التحديث
- **`services/update_service.py`** - خدمة التحقق والتنزيل

### 2. ملفات الواجهة
- **`ui/settings_tab.py`** - تم إضافة تاب "التحديثات" (معدل)

### 3. ملفات التوثيق
- **`BUILD_UPDATER.md`** - دليل المطور الكامل
- **`AUTO_UPDATE_GUIDE.md`** - دليل المستخدم
- **`UPDATE_SYSTEM_README.md`** - هذا الملف

### 4. ملفات مساعدة
- **`build_updater.bat`** - سكريبت بناء updater.exe
- **`test_update_system.py`** - اختبار النظام
- **`version.json.example`** - نموذج ملف التحديث

---

## 🚀 البدء السريع

### للمطور (إعداد لمرة واحدة):

1. **بناء updater.exe:**
   ```bash
   build_updater.bat
   ```

2. **إنشاء ملف version.json على GitHub:**
   ```json
   {
     "version": "1.0.1",
     "url": "https://github.com/USERNAME/REPO/releases/download/v1.0.1/update.zip"
   }
   ```

3. **تحديث رابط الفحص في version.py:**
   ```python
   UPDATE_CHECK_URL = "https://raw.githubusercontent.com/USERNAME/REPO/main/version.json"
   ```

### للمستخدم:

1. افتح البرنامج
2. اذهب إلى **الإعدادات → التحديثات**
3. اضغط **"التحقق من التحديثات"**
4. إذا وجد تحديث، اضغط **"تنزيل التحديث"**
5. بعد اكتمال التنزيل، اضغط **"تثبيت التحديث"**
6. سيتم إغلاق البرنامج وإعادة تشغيله تلقائياً

---

## 🔧 كيف يعمل النظام؟

### 1. التحقق من التحديثات
```
البرنامج → يقرأ version.json من GitHub → يقارن الإصدارات
```

### 2. تنزيل التحديث
```
البرنامج → ينزل update.zip → يحفظه في temp_update.zip
```

### 3. تطبيق التحديث
```
البرنامج → يشغل updater.exe → يغلق نفسه
updater.exe → ينتظر 3 ثواني → يفك الضغط → يشغل البرنامج → يغلق نفسه
```

---

## 📦 إصدار تحديث جديد

### الخطوات:

1. **تحديث رقم الإصدار:**
   ```python
   # في version.py
   CURRENT_VERSION = "1.0.1"
   ```

2. **تجهيز ملفات التحديث:**
   - اضغط جميع ملفات المشروع في `update.zip`
   - تأكد من تضمين جميع المجلدات (services, ui, core, assets)
   - لا تضمن قاعدة البيانات أو ملفات __pycache__

3. **رفع على GitHub Releases:**
   - أنشئ Release جديد (مثل: v1.0.1)
   - ارفع `update.zip`
   - انسخ رابط التنزيل المباشر

4. **تحديث version.json:**
   ```json
   {
     "version": "1.0.1",
     "url": "https://github.com/USERNAME/REPO/releases/download/v1.0.1/update.zip"
   }
   ```

5. **اختبار:**
   - شغل البرنامج
   - اذهب للإعدادات → التحديثات
   - اضغط "التحقق من التحديثات"

---

## 🧪 الاختبار

قبل إصدار أي تحديث، قم بتشغيل:

```bash
python test_update_system.py
```

يجب أن تنجح جميع الاختبارات (6/6).

---

## 🔍 استكشاف الأخطاء

### المشكلة: "لم يتم العثور على updater.exe"
**الحل:**
```bash
build_updater.bat
```

### المشكلة: "فشل الاتصال بالخادم"
**الحل:**
- تحقق من رابط UPDATE_CHECK_URL في version.py
- تأكد من وجود version.json على GitHub
- تحقق من الإنترنت

### المشكلة: "فشل التنزيل"
**الحل:**
- تأكد من صحة رابط التنزيل في version.json
- تأكد من وجود update.zip في GitHub Releases
- تحقق من مساحة القرص

### المشكلة: "فشل فك الضغط"
**الحل:**
- تأكد من أن ملف ZIP غير تالف
- تأكد من صلاحيات الكتابة
- أغلق جميع نوافذ البرنامج

---

## 📊 هيكل النظام

```
Sky Wave ERP/
├── version.py                    # رقم الإصدار
├── updater.py                    # المحدث
├── updater.exe                   # المحدث (مجمع)
├── services/
│   └── update_service.py         # خدمة التحديث
├── ui/
│   └── settings_tab.py           # تاب التحديثات (معدل)
├── BUILD_UPDATER.md              # دليل المطور
├── AUTO_UPDATE_GUIDE.md          # دليل المستخدم
├── build_updater.bat             # سكريبت البناء
├── test_update_system.py         # اختبار النظام
└── version.json.example          # نموذج
```

---

## 🎓 للمطورين

### إضافة ميزات جديدة:

1. **ملاحظات الإصدار:**
   ```python
   # في version.json
   {
     "version": "1.0.1",
     "url": "...",
     "release_notes": "تحسينات في الأداء"
   }
   ```

2. **التحقق من التوافق:**
   ```python
   # في version.json
   {
     "version": "1.0.1",
     "url": "...",
     "min_version": "1.0.0"
   }
   ```

3. **التحديث التلقائي عند البدء:**
   ```python
   # في main.py
   def check_updates_on_startup():
       # كود الفحص التلقائي
       pass
   ```

---

## ⚠️ ملاحظات مهمة

1. **النسخ الاحتياطي:**
   - دائماً أنشئ نسخة احتياطية قبل التحديث
   - البيانات في قاعدة البيانات آمنة

2. **الصلاحيات:**
   - قد تحتاج صلاحيات المسؤول
   - تأكد من إغلاق جميع نوافذ البرنامج

3. **التوافق:**
   - اختبر التحديث على بيئة تجريبية أولاً
   - تأكد من توافق الإصدار مع قاعدة البيانات

4. **الأمان:**
   - استخدم HTTPS في الروابط
   - تحقق من صحة الملفات المحملة

---

## ✅ قائمة التحقق

قبل إصدار تحديث:

- [ ] تحديث CURRENT_VERSION في version.py
- [ ] بناء update.zip بجميع الملفات
- [ ] رفع update.zip على GitHub Releases
- [ ] تحديث version.json
- [ ] اختبار التحديث على جهاز تجريبي
- [ ] التأكد من وجود updater.exe

---

## 📞 الدعم

- 📧 البريد: skywaveads@gmail.com
- 📱 الهاتف: +201067894321 - +201021965200
- 🌐 الموقع: www.skywaveads.com

---

## 📝 الترخيص

هذا النظام جزء من Sky Wave ERP وخاضع لنفس الترخيص.

---

**تم التطوير بواسطة Kiro AI** 🤖  
**Sky Wave ERP** - نظام إدارة متكامل 🚀
